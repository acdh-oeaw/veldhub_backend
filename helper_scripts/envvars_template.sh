#!/bin/bash
# copy this file to envvars.sh and modify that
# use this with `source envvars.sh`
export DB_HOST="REPLACE ME"
export DB_DATABASE="REPLACE ME"
export DB_USER="REPLACE ME"
export DB_PASSWORD="REPLACE ME"
